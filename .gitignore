# OS junk
.DS_Store
**/.DS_Store

# Editor/tooling
# Track the project skills, but keep machine-local settings out of the repo.
.claude/*
!.claude/skills/
!.claude/skills/**
.venv/

# Node tooling. Dev-only: the site itself has no build step.
node_modules/

# Internal tooling: never ship to the site.
content_picker_app.py
__pycache__/
deck/
